import { Counter } from './components/Counter';
import './index.scss';

export const App = () => {
    return (
        <div className='app'>
            test text
            <Counter />
        </div>
    );
};
